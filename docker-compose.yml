services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: databricks-poc
    container_name: databricks-poc
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__Databricks=${DATABRICKS_CONNECTION_STRING}
    restart: unless-stopped
